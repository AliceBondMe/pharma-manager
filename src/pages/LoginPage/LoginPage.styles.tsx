import { css } from '@emotion/react';
import { containerStyle } from '../RegisterPage/RegisterPage.styles';

export const loginContainerStyle = css`
  ${containerStyle}
  @media screen and (min-width: 1440px) {
    gap: 150px;
  }
`